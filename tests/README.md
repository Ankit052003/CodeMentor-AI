# Tests

Phase 2 creates the test folder so integration and end-to-end tests can be added in later phases. Phase 4 validation currently relies on TypeScript, linting, Prisma schema generation, and manual workflow checks.
